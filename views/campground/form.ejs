<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= campground ?'Edit Campground':'Create New Campground' %>
    </title>
</head>

<body>
    <h1>
        <%= campground?'Edit Campground':'Create New Campground' %>
    </h1>
    <form action="/campgrounds/edit?id=<%=campground?campground._id:undefined%>" method="POST">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" placeholder="<%= campground?"":'Campground Title' %>"
            value="<%= campground?campground.title:'' %>">

        <br>
        <label for="location">Location:</label>
        <input type="text" id="location" name="location" placeholder="<%= campground?"":'Campground Location' %>"
            value="<%= campground?campground.location:'' %>">

        <br>
        <label for="description">Description:</label>
        <input type="text" id="description" name="description"
            placeholder="<%= campground?"":'Campground description' %>"
            value="<%= campground?campground.description:'' %>">
        <button>Submit</button>
    </form>
</body>

</html>